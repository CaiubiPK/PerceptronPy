
N = 0.5
B = 0
Epoca = 1

while(true)
    Tamanho = input("Quantos parâmetros?")


    for(i=0;i<tamanho;i++)
    {
        //preencher os pesos de acordo com o numero de saidas
    }
    Tabela = input("Tabela de saída desejada, escreva 'P' para parar")



    V += saida[n].Tabela[n]

    if(B !- 0)
    V = V.B
    // conta com todos os parâmetros
    
    
    
    //Função de ativação
    
    
    
    
    
    //atualização de pesos
